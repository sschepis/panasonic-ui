import React from 'react';

import "../App.css";

const WaitSpinner = () => {
    return (
        <div className="wait-spinner">
            <div className="wait-spinner__spinner"></div>
        </div>
    );
};

export default WaitSpinner;
